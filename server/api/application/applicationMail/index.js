const invitePartner = require('./invitePartner')
const notifyPepite = require('./notifyPepite')
const notifyPepiteNew = require('./notifyPepiteNew')
const notifyPepiteOld = require('./notifyPepiteOld')
const notifyTutor = require('./notifyTutor')
const saveApplication = require('./saveApplication')
const sendApplication = require('./sendApplication')
const transferApplication = require('./transferApplication')

module.exports = {
  invitePartner,
  notifyPepite,
  notifyPepiteNew,
  notifyPepiteOld,
  notifyTutor,
  saveApplication,
  sendApplication,
  transferApplication
}
